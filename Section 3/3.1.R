owners <- data.frame(
  owner = c("Alice", "Bob", "Carol", "Dan", "Erin"),
  pet = c("Snowy", "Mittens", "Mittens", "Goldie", "Pancho"),
  stringsAsFactors = F
)

species <- data.frame(
  pet = c("Fido", "Goldie", "Mittens", "Rex", "Snowy"),
  species = c("Dog", "Goldfish", "Cat", "Dog", "Dog"),
  stringsAsFactors = F
)